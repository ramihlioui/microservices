import { CrudToastDirectiveDirective } from './crud-toast-directive.directive';

describe('CrudToastDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CrudToastDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
